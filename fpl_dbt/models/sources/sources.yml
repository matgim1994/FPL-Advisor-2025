sources:
  - name: raw
    tables:
      - name: elements
        description: "Raw players data."
      - name: events
        description: "Raw data on each gameweek."
      - name: teams
        description: "Raw teams data."
      - name: fixtures
        description: "Raw fixtures data."
      - name: players_history
        description: "Raw players history data."
      - name: players_fixtures
        description: "Raw players fixtures data."